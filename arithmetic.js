const sum=5+3; //addition
const diff=10-2; //substraction
const p=4*3; //multiplication
const q=8/2; //division
const r=3%2; //remainder
console.log("here is the addition, subtraction, multiplication, division, remainder result: ", sum, diff, p, q, r);
//assignment operators
let n=10;
n+=5;
n*=2;
console.log("the value of n is:" ,n);